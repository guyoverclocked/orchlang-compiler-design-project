\documentclass[tikz,border=2pt]{standalone}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usetikzlibrary{positioning,arrows.meta,fit,calc,backgrounds}
\begin{document}
\begin{tikzpicture}[
    font=\small,
    stage/.style={draw, rounded corners=2pt, fill=gray!8, align=center,
                  minimum height=10mm, inner sep=3pt, text width=24mm},
    art/.style={draw, align=center, minimum height=8mm, inner sep=3pt, text width=20mm},
    flow/.style={-{Stealth[length=2mm]}, thick},
    check/.style={-{Stealth[length=2mm]}, dashed},
    note/.style={font=\scriptsize, align=center, text=black!70}]
  % front end
  \node[art] (src) {workflow\\\texttt{.orch}};
  \node[stage, right=6mm of src] (parse) {parser, binding\\identities};
  \node[stage, right=6mm of parse] (types) {types and\\labels (\S\ref*{sec:language})};
  % analyses
  \node[stage, above right=2mm and 8mm of types] (cost) {cost bound\\(bytes, contracts)};
  \node[stage, below right=2mm and 8mm of types] (rel) {request signatures\\and leakage};
  \node[art, right=24mm of types] (cert) {certificate\\(JSON)};
  \draw[flow] (src) -- (parse);
  \draw[flow] (parse) -- (types);
  \draw[flow] (types.east) -- ++(3mm,0) |- (cost.west);
  \draw[flow] (types.east) -- ++(3mm,0) |- (rel.west);
  \draw[flow] (cost.east) -- ++(3mm,0) |- (cert.west);
  \draw[flow] (rel.east) -- ++(3mm,0) |- (cert.west);
  % runtime and harness
  \node[stage, below=16mm of rel, text width=38mm] (run) {runtime: content-dependent\\provider, three couplings,\\non-subadditive tokenizer};
  \node[stage, left=10mm of run, text width=30mm] (harness) {harness\\\texttt{bench/evaluate.py}};
  \node[art, right=10mm of run, text width=22mm] (real) {real tokenizers,\\a real model};
  \draw[flow] (harness) -- node[note, above]{runs} (run);
  \draw[flow] (real) -- node[note, above]{re-bill} (run);
  \draw[check] (cert.south) |- node[note, pos=0.25, right]{checked\\against runs} ($(run.north east)+(0,2mm)$);
  % Coq
  \node[art, above=8mm of cost, text width=34mm] (coq) {Coq model: Lemma~2,\\Theorems 1, 5 and 6};
  \draw[check] (coq) -- node[note, right]{same rules} (cost);
\end{tikzpicture}
\end{document}
