# Generated compiler outputs
/build/
/orchc
/orchlang_tests
*.o
*.d

# Local operating-system metadata
.DS_Store

# local toolchain helper
.envrc
*.exe

# Node dependencies for the submission document generators.
# package.json is tracked so `npm install` alone is enough.
node_modules/
package-lock.json

# Downloaded tokenizers, corpora and models for the measurement scripts.
bench/.cache/

# Coq build products.
proofs/*.vo
proofs/*.vok
proofs/*.vos
proofs/*.glob
proofs/.*.aux
.lia.cache
