Workflow SupportTriage budget=2500
  Input ticket : text
  Secret API_KEY
  Model fast provider=mock name=local-small max_tokens=600
  Prompt classify(message:text) -> text
  Let category : text
    Call classify(ticket) using fast
  Require tokens(category) <= 600
  Output category
