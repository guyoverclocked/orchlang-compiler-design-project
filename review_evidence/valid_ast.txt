Workflow ResearchDigest budget=2400
  Input query : text [public/trusted]
  Input web_page : text [public/untrusted]
  Model reader provider=mock name=offline-reader max_tokens=500
  Tool publish(body:text)
  Prompt digest(page:text) -> text
  Let digest_text : text
    Call digest(web_page) using reader
  Endorse digest_text as vetted : text because "passed the offline schema and length validator"
  Emit publish(vetted)
  Output query
