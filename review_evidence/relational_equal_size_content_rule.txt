audit/2026-09-25/equal_size_template.orch:12:3: error [E236] the guard 'enterprise' depends on a secret and the two arms send different requests, so the trace observer can tell which arm ran; at step 1 the arms send different request text to the same endpoint, and a provider's response length depends on what a request says, not only on its size. then-arm sends [offline-m("Escalate in detail!: " + ticket)] and else-arm sends [offline-m("Acknowledge briefly: " + ticket)]
audit/2026-09-25/equal_size_template.orch:6:1: error [E236] workflow 'EqualSizeTemplate' has 2 distinguishable behaviours depending on its secrets, so it may reveal up to 1 bits; it declares no leakage budget, so none is allowed
