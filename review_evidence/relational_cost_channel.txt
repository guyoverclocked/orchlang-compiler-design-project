examples/invalid/cost_channel.orch:14:3: error [E236] the guard 'ALERT' depends on a secret and the two arms send different requests, so the trace observer can tell which arm ran; at step 1 the arms call different endpoints: mock:offline-large max_tokens=900 and mock:offline-small max_tokens=150. then-arm sends [offline-large("Process: " + src)] and else-arm sends [offline-small("Process: " + src)]
examples/invalid/cost_channel.orch:6:1: error [E236] workflow 'SecretCostChannel' has 2 distinguishable behaviours depending on its secrets, so it may reveal up to 1 bits; it declares no leakage budget, so none is allowed
