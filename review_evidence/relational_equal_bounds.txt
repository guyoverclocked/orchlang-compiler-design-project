examples/invalid/equal_bounds.orch:17:2: error [E236] the guard 'tokens(s) == 0' depends on a secret and the two arms send different requests, so the trace observer can tell which arm ran; at step 1 the arms send different request text to the same endpoint, and a provider's response length depends on what a request says, not only on its size. then-arm sends [m(x)] and else-arm sends [m(y)]
examples/invalid/equal_bounds.orch:11:1: error [E236] workflow 'EqualBounds' has 2 distinguishable behaviours depending on its secrets, so it may reveal up to 1 bits; it declares no leakage budget, so none is allowed
